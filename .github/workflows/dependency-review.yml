name: dependency-review

# Blocks a PR that introduces a vulnerable or disallowed-license dependency, before it merges.
on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: high
          comment-summary-in-pr: on-failure
          allow-licenses: >-
            MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD,
            CC0-1.0, CC-BY-4.0, Unlicense, MS-PL, MIT-0, Python-2.0,
            BlueOak-1.0.0
